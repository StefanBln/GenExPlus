# Requires PostgreSQL (db1.* in application.properties or REPORT_DB1_* env).
database.id=db1
report.database.optional=false
report.template=test-template.jrxml
report.output.dir=__OUTPUT_DIR__
report.output.filename=db-report.pdf
report.format=PDF
report.parameter.ReportTitle=Database E2E
report.parameter.Author=GenExPlus
report.timestamp.auto=false

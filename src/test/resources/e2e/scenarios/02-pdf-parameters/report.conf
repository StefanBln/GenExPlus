# PDF with multiple parameters (still DB-less, uses simple.jrxml).
report.database.optional=true
report.template=simple.jrxml
report.output.dir=__OUTPUT_DIR__
report.output.filename=parameters.pdf
report.format=PDF
report.parameter.title=Quarterly Summary
report.parameter.date={[yyyy-MM-dd]}
report.timestamp.auto=false

# Unknown profile — should fail validation (exit 1) before JDBC connect.
database.id=db99
report.database.optional=false
report.template=test-template.jrxml
report.output.dir=__OUTPUT_DIR__
report.output.filename=missing-db.pdf
report.format=PDF
report.parameter.ReportTitle=Missing DB
report.timestamp.auto=false

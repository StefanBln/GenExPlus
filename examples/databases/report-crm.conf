# CRM report — uses MySQL profile db2.
database.id=db2
report.database.optional=false
report.template=templates/crm-pipeline.jrxml
report.output.dir=genexplus-output
report.output.filename=crm-pipeline.pdf
report.format=PDF
report.parameter.ReportTitle=CRM Pipeline
report.parameter.Author=GenExPlus
report.timestamp.auto=false
